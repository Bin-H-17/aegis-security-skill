# Smoke-test fixture. Copy to .env to demo secret scanning.
# Values are intentionally non-sensitive placeholders (won't trip GitHub push protection).
AWS_ACCESS_KEY_ID=EXAMPLE_AKIA_REPLACE_ME
DB_PASSWORD=CHANGE_ME_EXAMPLE
